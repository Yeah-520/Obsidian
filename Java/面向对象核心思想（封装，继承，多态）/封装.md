# 封装
>把对象的**属性**和**行为** “包装” 起来，对外**隐藏内部**细节，只暴露必要的接口供外部使用(**私有化字段 + 提供公共方法**)
1. **数据安全**
	- 在方法中做数据校验，避免非法值
	- **对成员变量进行更精确的控制**
2. **隐藏内部**细节
	- 隐藏对象内部的实现逻辑方法、数据处理方式
	- 不需要对外暴露的变量 / 方法
3. **易维护、低耦合**
	- 便于后续修改内部逻辑（比如把年龄校验规则从「0-150」改成「0-120」），外部调用方完全不用改
	- 对成员进行更精确的控制
---